var courseDatas = require('./courseDatas');
var coursesList = require('./coursesList');
var deptList = require('./deptList');
var fuzzySearch = require('./fuzzySearch');

module.exports = {
  courseDatas: courseDatas,
  coursesList: coursesList,
  deptList: deptList,
  fuzzySearch: fuzzySearch
}
